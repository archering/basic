;(function(){
    //convert to string
    //convert to number
    //convert to boolean

    //alert([1,2,3] + " "); //1,2,3     + 和 字符串相连，两边都要是字符串，不是的转
    //alert(+[1,2,3]); //需要转成一个数字，但是【】是个数组，没有valueOf return SNaN
    //alert([1,2,3] == 1);// NaN == 1   FALSE
    //alert(parseInt([1,2,4]) == 1);// true
    //alert(['x'] == 'x'); //comparsion, need convert to number,  right already a primitive value

}());
